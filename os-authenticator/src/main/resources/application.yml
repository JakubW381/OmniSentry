server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}/auth-db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true